<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mongodb.stitch.android.services.mongodb.local">

    <application>
        <!-- MongoDbMobileProvider initialize MongoDB Mobile for future usage -->
        <provider
            android:name=".internal.MongoDbMobileProvider"
            android:authorities="${applicationId}.StitchMongoDBMobileProvider"
            android:exported="false" />
    </application>
</manifest>
